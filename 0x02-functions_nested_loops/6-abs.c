#include "main.h"

/**
 * _abs - print the absolute value of the r
 * @r: An integre input
 * Return: absolute value
 * Description: This function returns absolute value of a number
 */

int _abs(int)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-1 * r);
	}
}
